to do list

    Spinner page

    Review page
        - "select subject" and "select week" popups don't go away when i tap on a subject. 
        - "select subject" and "select week" popups are light when in dark mode.

    Lessons page
        - "reset grid" popup in dark mode is not dark so the white text doesn't show up
        - grid colours on dark mode. the lines and blocks blend together looking like a blob.
    
    Settings page
        - get rid of large text setting? seems to be too much for the space allowed.

Future updates

    - Add the audio files to each lesson
    - focus lesson (mark a lesson the child struggles with for easier review)
    - add a challenge mode for timed review that counts the lessons you complete in a select amount of time. (celebration at the end)    